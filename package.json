{
  "name": "deeper-link",
  "version": "0.3.0",
  "private": true,
  "description": "Turn fragile, login-order-dependent links (e.g. Gmail /u/0/) into stable ones.",
  "type": "module",
  "packageManager": "pnpm@12.6.0",
  "scripts": {
    "build": "node ts/chrome-extension/build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
